#why does a client needs to know about the server registry, so using false
eureka.client.fetch-registry=false
#If you want to discover other microservices but you yourself as a microservice not discoverable by others, then use false
eureka.client.register-with-eureka=false
#just using the same default eureka server port
server.port=8761
eureka.instance.hostname=peer1
eureka.client.serviceUrl.defaultZone=http://peer2:8762/
#serviceUrls={test1:'test1',test2:'test2'}